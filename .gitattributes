# Treat images as binary (prevents corruption or unwanted processing)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary

# Avoid text-based line ending normalization for binaries
*.png -text
*.jpg -text
*.jpeg -text
*.gif -text

# Force LF endings for source code (optional best practice)
*.js text eol=lf
*.css text eol=lf
*.html text eol=lf
*.md text eol=lf
